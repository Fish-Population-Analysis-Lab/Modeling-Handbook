{
  "hash": "1eaacc49aec3e2084ed32b4221a1adb1",
  "result": {
    "engine": "knitr",
    "markdown": "# MSE\n\n\n\n\n\n\n\n\n\n\n\n## Harvest control rules (HCRs) \n\n\n:::: columns\n\n\n::: {.column width=\"32%\"}\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Load necessary library\nlibrary(ggplot2)\nlibrary(grid)\n\n# Define key biomass points\nBlim <- 0.3        # Limit biomass level (Blim)\nBtrigger <- 0.5    # Trigger biomass level (Btrigger)\nBmax <- 1.0        # Maximum biomass (100% of unfished biomass)\nFMSY <- 1.0        # Maximum sustainable fishing mortality\n\n# Create a sequence of biomass values\nB_values <- seq(0, Bmax, length.out = 100)\n\n# Compute fishing mortality based on the HCR rule\nF_values <- ifelse(B_values < Blim, 0,\n                   ifelse(B_values <= Btrigger, FMSY * (B_values - Blim) / (Btrigger - Blim), \n                          FMSY))\n\n# Create a data frame\ndf <- data.frame(Biomass = B_values, Fishing_Mortality = F_values)\n\n# Generate the plot\nggplot(df, aes(x = Biomass, y = Fishing_Mortality)) + \n  \n  # HCR Curve\n  geom_line(color = blue, size = 5, aes(linetype = \"HCR Curve\")) +\n  \n  # Reference points as vertical dashed lines\n  geom_vline(aes(xintercept = Blim, color = \"Blim\"), linetype = \"dashed\", size = 2) +\n  geom_vline(aes(xintercept = Btrigger, color = \"Btrigger\"), linetype = \"dashed\", size = 2) +\n  \n  # Labels and legend\n  labs(\n    title = \"HCR A\", \n    x = expression(B  ~ bold(\"/\") ~  B[MSY]), \n    y = expression(F  ~ bold(\"/\") ~  F[MSY]),\n    linetype = \"HCR Components\", \n    color = NULL  # Remove color legend title\n  ) +\n  \n  # Scale colors for legend\n  scale_color_manual(values = c(\"Blim\" = \"red\", \n                                \"Btrigger\" = \"green\")) +\n  \n  # Set y-axis breaks to only show 0.0, 0.5, and 1.0\n  scale_y_continuous(breaks = c(0.0, 0.5, 1.0)) +\n  \n  # Set x-axis breaks to include Blim and Btrigger explicitly\n  scale_x_continuous(breaks = c(0.0, 0.3, 0.5, 1.0)) +\n  \n  # Custom theme\n  #theme_minimal() +\n  \n  # Theme adjustments\n  theme(\n    # Titles and Axis Labels\n    plot.title = element_text(hjust = 0.5, face = \"bold\", size = 35, colour = blue),\n    axis.title = element_text(face = \"bold\", size = 35),\n    axis.text.x = element_text(size = 28, face = \"bold\"),\n    axis.text.y = element_text(size = 28, face = \"bold\"),\n    \n    # Legend Positioning and Style\n    legend.position = c(0.88, 0.89),  # Position legend inside the plot\n    legend.justification = c(1, 1),    \n    legend.title = element_blank(),    \n    legend.text = element_text(face = \"bold\", size = 8),  \n    legend.background = element_rect(fill = \"white\", colour = \"black\", linewidth = 0.5),\n    legend.key = element_rect(fill = \"white\"),\n    legend.key.height = unit(0.25, \"line\"),\n    legend.key.width = unit(0.8, \"line\"),\n    legend.key.size = grid::unit(0.2, \"lines\"),\n    legend.spacing.x = unit(0.05, \"cm\"),\n    legend.spacing.y = unit(0.05, \"cm\"),\n    \n    # Grid, Ticks, and Border\n    panel.grid.major = element_blank(),\n    panel.grid.minor = element_blank(),\n    panel.background = element_blank(),\n    panel.border = element_rect(fill = NA, colour = \"grey45\", linewidth = 4),\n    \n    # Bold Formatting for Various Text Elements\n    text = element_text(face = \"bold\"),\n    plot.subtitle = element_text(face = \"bold\"),\n    plot.caption = element_text(face = \"bold\")\n  ) +\n  theme(\n  axis.ticks = element_line(linewidth = 2, color = \"grey45\"),  # Makes axis ticks bolder\n  axis.ticks.length = grid::unit(20 / 2.2, \"pt\"),  # Increases tick length\n  \n  strip.background = element_rect(fill = \"grey45\", color = \"grey45\", linewidth = 2),  # Thicker strip lines\n  strip.text = element_text(face = \"bold\", size = rel(1.8))  # Increase strip text size and boldness\n)+\n\n\n\n  # Combine linetype and color into a single legend frame\n  guides(\n    color = guide_legend(override.aes = list(linetype = 1)),\n    linetype = guide_legend(override.aes = list(color = blue))\n  ) +\n  \n  # Add mathematical formula annotation\n  annotation_custom(\n    grob = textGrob(\n      label = expression(F == F[MSY] * frac(B - B[lim], B[trigger] - B[lim])),\n      gp = gpar(fontsize = 25, fontface = \"italic\"),\n      hjust = 0, vjust = 1\n    ),\n    xmin = 0.5, xmax = 0.64, ymin = 0.3, ymax = 0.4  # Adjust the placement of the annotation\n  )\n```\n\n::: {.cell-output-display}\n![](MSE_files/figure-html/unnamed-chunk-5-1.png){width=100%}\n:::\n:::\n\n:::\n\n::: {.column width=\"32%\"}\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Load necessary library\nlibrary(ggplot2)\nlibrary(grid)\n\n# Define key biomass points\nBlim <- 0.3        # Limit biomass level (Blim)\nBtrigger <- 0.5    # Trigger biomass level (Btrigger)\nBmax <- 1.0        # Maximum biomass (100% of unfished biomass)\nFMSY <- 1.0        # Maximum sustainable fishing mortality\n\n# Create a sequence of biomass values\nB_values <- seq(0, Bmax, length.out = 100)\n\n# Compute fishing mortality based on the HCR B rule\nF_values_B <- ifelse(B_values < Blim, 0,\n                     ifelse(B_values <= Btrigger, \n                            FMSY * (0.5 + 0.5 * (B_values - Blim) / (Btrigger - Blim)), \n                            FMSY))\n\n# Create a data frame\ndf_B <- data.frame(Biomass = B_values, Fishing_Mortality = F_values_B)\n\n# Generate the plot\nggplot(df_B, aes(x = Biomass, y = Fishing_Mortality)) +\n  \n  # HCR B Curve\n  geom_line(color = purple, size = 5, aes(linetype = \"HCR Curve\")) +\n  \n  # Reference points as vertical dashed lines\n  geom_vline(aes(xintercept = Blim, color = \"Blim\"), linetype = \"dashed\", size = 2) +\n  geom_vline(aes(xintercept = Btrigger, color = \"Btrigger\"), linetype = \"dashed\", size = 2) +\n  \n  # Labels and legend\n  labs(\n    title = \"HCR B\", \n    x = expression(B  ~ bold(\"/\") ~  B[MSY]), \n    y = expression(F  ~ bold(\"/\") ~  F[MSY]),\n    linetype = \"HCR Components\", \n    color = NULL  # Remove color legend title\n  ) +\n  \n  # Scale colors for legend\n  scale_color_manual(values = c(\"Blim\" = \"red\", \n                                \"Btrigger\" = \"green\")) +\n  \n  # Set y-axis breaks to only show 0.0, 0.5, and 1.0\n  scale_y_continuous(breaks = c(0.0, 0.5, 1.0)) +\n  \n  # Set x-axis breaks to include Blim and Btrigger explicitly\n  scale_x_continuous(breaks = c(0.0, 0.3, 0.5, 1.0)) +\n  \n  # Custom theme\n  #theme_minimal() +\n  \n  # Theme adjustments\n  theme(\n    # Titles and Axis Labels\n    plot.title = element_text(hjust = 0.5, face = \"bold\", size = 35, colour = purple),\n    axis.title = element_text(face = \"bold\", size = 35),\n    axis.text.x = element_text(size = 28, face = \"bold\"),\n    axis.text.y = element_text(size = 28, face = \"bold\"),\n    \n    # Legend Positioning and Style\n    legend.position = c(0.88, 0.89),  # Position legend inside the plot\n    legend.justification = c(1, 1),    \n    legend.title = element_blank(),    \n    legend.text = element_text(face = \"bold\", size = 8),  \n    legend.background = element_rect(fill = \"white\", colour = \"black\", linewidth = 0.5),\n    legend.key = element_rect(fill = \"white\"),\n    legend.key.height = unit(0.25, \"line\"),\n    legend.key.width = unit(0.8, \"line\"),\n    legend.key.size = grid::unit(0.2, \"lines\"),\n    legend.spacing.x = unit(0.05, \"cm\"),\n    legend.spacing.y = unit(0.05, \"cm\"),\n    \n    # Grid, Ticks, and Border\n    panel.grid.major = element_blank(),\n    panel.grid.minor = element_blank(),\n    panel.background = element_blank(),\n    panel.border = element_rect(fill = NA, colour = \"grey45\", linewidth = 4),\n    \n    # Bold Formatting for Various Text Elements\n    text = element_text(face = \"bold\"),\n    plot.subtitle = element_text(face = \"bold\"),\n    plot.caption = element_text(face = \"bold\")\n  ) +\n  theme(\n  axis.ticks = element_line(linewidth = 2, color = \"grey45\"),  # Makes axis ticks bolder\n  axis.ticks.length = grid::unit(20 / 2.2, \"pt\"),  # Increases tick length\n  \n  strip.background = element_rect(fill = \"grey45\", color = \"grey45\", linewidth = 2),  # Thicker strip lines\n  strip.text = element_text(face = \"bold\", size = rel(1.8))  # Increase strip text size and boldness\n)+\n  \n  # Combine linetype and color into a single legend frame\n  guides(\n    color = guide_legend(override.aes = list(linetype = 1)),\n    linetype = guide_legend(override.aes = list(color = purple))\n  ) +\n  \n  # Add mathematical formula annotation\n  annotation_custom(\n    grob = textGrob(\n      label = expression(F == F[MSY] * (0.5 + 0.5 * frac(B - B[lim], B[trigger] - B[lim]))),\n      gp = gpar(fontsize = 25, fontface = \"italic\"),\n      hjust = 0, vjust = 1\n    ),\n    xmin = 0.35, xmax = 0.48, ymin = 0.3, ymax = 0.4  # Adjust the placement of the annotation\n  )\n```\n\n::: {.cell-output-display}\n![](MSE_files/figure-html/unnamed-chunk-6-1.png){width=100%}\n:::\n:::\n\n:::\n\n::: {.column width=\"32%\"}\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Load necessary libraries\nlibrary(ggplot2)\nlibrary(grid)\n\n# Define key biomass points\nBlim <- 0.0        # Minimum biomass level (Blim is 0 in this case)\nBtrigger <- 0.5    # Trigger biomass level (Btrigger)\nBmax <- 1.0        # Maximum biomass (100% of unfished biomass)\nFMSY <- 1.0        # Maximum sustainable fishing mortality\n\n# Create a sequence of biomass values\nB_values <- seq(0, Bmax, length.out = 100)\n\n# Compute fishing mortality based on the HCR C rule\nF_values_C <- ifelse(B_values <= Btrigger, \n                     FMSY * (B_values / Btrigger), \n                     FMSY)\n\n# Create a data frame\ndf_C <- data.frame(Biomass = B_values, Fishing_Mortality = F_values_C)\n\n# Generate the plot\nggplot(df_C, aes(x = Biomass, y = Fishing_Mortality)) +\n  \n  # HCR C Curve\n  geom_line(color = red, size = 5, aes(linetype = \"HCR Curve\")) +\n  \n  # Reference points as vertical dashed lines\n  geom_vline(aes(xintercept = Btrigger, color = \"Btrigger\"), linetype = \"dashed\", size = 2) +\n  \n  \n  \n  # Labels and legend\n  labs(\n    title = \"HCR C\", \n    x = expression(B  ~ bold(\"/\") ~  B[MSY]), \n    y = expression(F  ~ bold(\"/\") ~  F[MSY]),\n    linetype = \"HCR Components\", \n    color = NULL  # Remove color legend title\n  ) +\n  \n  # Scale colors for legend\n  scale_color_manual(values = c(\"Btrigger\" = \"green\")) +\n  \n  # Set y-axis breaks to only show 0.0, 0.5, and 1.0\n  scale_y_continuous(breaks = c(0.0, 0.5, 1.0)) +\n  \n  # Set x-axis breaks to include Blim and Btrigger explicitly\n  scale_x_continuous(breaks = c(0.0, 0.5, 1.0)) +\n  \n  # Custom theme\n  #theme_minimal() +\n  \n  # Theme adjustments\n  theme(\n    # Titles and Axis Labels\n    plot.title = element_text(hjust = 0.5, face = \"bold\", size = 35, colour = red),\n    axis.title = element_text(face = \"bold\", size = 35),\n    axis.text.x = element_text(size = 28, face = \"bold\"),\n    axis.text.y = element_text(size = 28, face = \"bold\"),\n    \n    # Legend Positioning and Style\n    legend.position = c(0.88, 0.89),  # Position legend inside the plot\n    legend.justification = c(1, 1),    \n    legend.title = element_blank(),    \n    legend.text = element_text(face = \"bold\", size = 8),  \n    legend.background = element_rect(fill = \"white\", colour = \"black\", linewidth = 0.5),\n    legend.key = element_rect(fill = \"white\"),\n    legend.key.height = unit(0.25, \"line\"),\n    legend.key.width = unit(0.8, \"line\"),\n    legend.key.size = grid::unit(0.2, \"lines\"),\n    legend.spacing.x = unit(0.05, \"cm\"),\n    legend.spacing.y = unit(0.05, \"cm\"),\n    \n    # Grid, Ticks, and Border\n    panel.grid.major = element_blank(),\n    panel.grid.minor = element_blank(),\n    panel.background = element_blank(),\n    panel.border = element_rect(fill = NA, colour = \"grey45\", linewidth = 4),\n    \n    # Bold Formatting for Various Text Elements\n    text = element_text(face = \"bold\"),\n    plot.subtitle = element_text(face = \"bold\"),\n    plot.caption = element_text(face = \"bold\")\n  ) +\n  theme(\n  axis.ticks = element_line(linewidth = 2, color = \"grey45\"),  # Makes axis ticks bolder\n  axis.ticks.length = grid::unit(20 / 2.2, \"pt\"),  # Increases tick length\n  \n  strip.background = element_rect(fill = \"grey45\", color = \"grey45\", linewidth = 2),  # Thicker strip lines\n  strip.text = element_text(face = \"bold\", size = rel(1.8))  # Increase strip text size and boldness\n)+\n  \n  # Combine linetype and color into a single legend frame\n  guides(\n    color = guide_legend(override.aes = list(linetype = 1)),\n    linetype = guide_legend(override.aes = list(color = red))\n  ) +\n  \n  # Add mathematical formula annotation\n  annotation_custom(\n    grob = textGrob(\n      label = expression(F == F[MSY] * frac(B, B[trigger])),\n      gp = gpar(fontsize = 25, fontface = \"italic\"),\n      hjust = 0, vjust = 1\n    ),\n    xmin = 0.6, xmax = 0.65, ymin = 0.3, ymax = 0.4  # Adjust the placement of the annotation\n  )\n```\n\n::: {.cell-output-display}\n![](MSE_files/figure-html/unnamed-chunk-7-1.png){width=100%}\n:::\n:::\n\n:::\n::::\n\n:::: columns \n\n::: {.column width=\"32%\"}\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Load necessary library\nlibrary(ggplot2)\nlibrary(grid)\n\n# Define key biomass points\nBlim <- 0.3        # Limit biomass level (Blim)\nBtrigger <- 0.5    # Trigger biomass level (Btrigger)\nBmax <- 1.0        # Maximum biomass (100% of unfished biomass)\nFMSY <- 1.0        # Maximum sustainable fishing mortality\nFtarget <- 0.8 * FMSY  # 80% of FMSY\n\n# Create a sequence of biomass values\nB_values <- seq(0, Bmax, length.out = 100)\n\n# Compute fishing mortality based on the HCR D80 rule\nF_values <- ifelse(B_values < Blim, 0,\n                   ifelse(B_values <= Btrigger, Ftarget * (B_values - Blim) / (Btrigger - Blim), \n                          Ftarget))\n\n# Create a data frame\ndf <- data.frame(Biomass = B_values, Fishing_Mortality = F_values)\n\n# Generate the plot\nggplot(df, aes(x = Biomass, y = Fishing_Mortality)) +\n  \n  # HCR D80 Curve\n  geom_line(color = orange, size = 5, aes(linetype = \"HCR Curve\")) +\n  \n  # Reference points as vertical dashed lines\n  geom_vline(aes(xintercept = Blim, color = \"Blim\"), linetype = \"dashed\", size = 2) +\n  geom_vline(aes(xintercept = Btrigger, color = \"Btrigger\"), linetype = \"dashed\", size = 2) +\n  \n  # Labels and legend\n  labs(\n    title = \"HCR D80\", \n    x = expression(B  ~ bold(\"/\") ~  B[MSY]), \n    y = expression(F  ~ bold(\"/\") ~  F[MSY]),\n    linetype = \"HCR Components\", \n    color = NULL  # Remove color legend title\n  ) +\n  \n  # Scale colors for legend\n  scale_color_manual(values = c(\"Blim\" = \"red\", \n                                \"Btrigger\" = \"green\")) +\n  \n  # Set y-axis breaks to only show 0.0, 0.4, and 0.8\n  #scale_y_continuous(breaks = c(0.0, 0.4, 0.8)) +\n  scale_y_continuous(breaks = c(0.0, 0.5, 0.8, 1.0), limits = c(0, 1))+\n  \n  # Set x-axis breaks to include Blim and Btrigger explicitly\n  scale_x_continuous(breaks = c(0.0, 0.3, 0.5, 1.0)) +\n  \n  # Custom theme\n  #theme_minimal() +\n  \n  # Theme adjustments\n  theme(\n    # Titles and Axis Labels\n    plot.title = element_text(hjust = 0.5, face = \"bold\", size = 35, colour = orange),\n    axis.title = element_text(face = \"bold\", size = 35),\n    axis.text.x = element_text(size = 28, face = \"bold\"),\n    axis.text.y = element_text(size = 28, face = \"bold\"),\n    \n    # Legend Positioning and Style\n    legend.position = c(0.88, 0.72),  # Position legend inside the plot\n    legend.justification = c(1, 1),    \n    legend.title = element_blank(),    \n    legend.text = element_text(face = \"bold\", size = 8),  \n    legend.background = element_rect(fill = \"white\", colour = \"black\", linewidth = 0.5),\n    legend.key = element_rect(fill = \"white\"),\n    legend.key.height = unit(0.25, \"line\"),\n    legend.key.width = unit(0.8, \"line\"),\n    legend.key.size = grid::unit(0.2, \"lines\"),\n    legend.spacing.x = unit(0.05, \"cm\"),\n    legend.spacing.y = unit(0.05, \"cm\"),\n    \n    # Grid, Ticks, and Border\n    panel.grid.major = element_blank(),\n    panel.grid.minor = element_blank(),\n    panel.background = element_blank(),\n    panel.border = element_rect(fill = NA, colour = \"grey45\", linewidth = 4),\n    \n    # Bold Formatting for Various Text Elements\n    text = element_text(face = \"bold\"),\n    plot.subtitle = element_text(face = \"bold\"),\n    plot.caption = element_text(face = \"bold\")\n  ) +\n  theme(\n  axis.ticks = element_line(linewidth = 2, color = \"grey45\"),  # Makes axis ticks bolder\n  axis.ticks.length = grid::unit(20 / 2.2, \"pt\"),  # Increases tick length\n  \n  strip.background = element_rect(fill = \"grey45\", color = \"grey45\", linewidth = 2),  # Thicker strip lines\n  strip.text = element_text(face = \"bold\", size = rel(1.8))  # Increase strip text size and boldness\n)+\n  \n  # Combine linetype and color into a single legend frame\n  guides(\n    color = guide_legend(override.aes = list(linetype = 1)),\n    linetype = guide_legend(override.aes = list(color = orange))\n  ) +\n  \n  # Add mathematical formula annotation\n  annotation_custom(\n    grob = textGrob(\n      label = expression(F == 0.8 * F[MSY] * frac(B - B[lim], B[trigger] - B[lim])),\n      gp = gpar(fontsize = 25, fontface = \"italic\"),\n      hjust = 0, vjust = 1\n    ),\n    xmin = 0.5, xmax = 0.56, ymin = 0.3, ymax = 0.4  # Adjust the placement of the annotation\n  )\n```\n\n::: {.cell-output-display}\n![](MSE_files/figure-html/unnamed-chunk-8-1.png){width=100%}\n:::\n:::\n\n:::\n\n::: {.column width=\"32%\"}\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Load necessary library\nlibrary(ggplot2)\nlibrary(grid)\n\n# Define key biomass points\nBlim <- 0.3        # Limit biomass level (Blim)\nBtrigger <- 0.5    # Trigger biomass level (Btrigger)\nBmax <- 1.0        # Maximum biomass (100% of unfished biomass)\nFMSY <- 1.0        # Maximum sustainable fishing mortality\nFtarget <- 0.9 * FMSY  # 80% of FMSY\n\n# Create a sequence of biomass values\nB_values <- seq(0, Bmax, length.out = 100)\n\n# Compute fishing mortality based on the HCR D80 rule\nF_values <- ifelse(B_values < Blim, 0,\n                   ifelse(B_values <= Btrigger, Ftarget * (B_values - Blim) / (Btrigger - Blim), \n                          Ftarget))\n\n# Create a data frame\ndf <- data.frame(Biomass = B_values, Fishing_Mortality = F_values)\n\n# Generate the plot\nggplot(df, aes(x = Biomass, y = Fishing_Mortality)) +\n  \n  # HCR D80 Curve\n  geom_line(color = Cyan, size = 5, aes(linetype = \"HCR Curve\")) +\n  \n  # Reference points as vertical dashed lines\n  geom_vline(aes(xintercept = Blim, color = \"Blim\"), linetype = \"dashed\", size = 2) +\n  geom_vline(aes(xintercept = Btrigger, color = \"Btrigger\"), linetype = \"dashed\", size = 2) +\n  \n  \n  # Labels and legend\n  labs(\n    title = \"HCR D90\", \n    x = expression(B  ~ bold(\"/\") ~  B[MSY]), \n    y = expression(F  ~ bold(\"/\") ~  F[MSY]),\n    linetype = \"HCR Components\", \n    color = NULL  # Remove color legend title\n  ) +\n  \n  # Scale colors for legend\n  scale_color_manual(values = c(\"Blim\" = \"red\", \n                                \"Btrigger\" = \"green\")) +\n  \n  # Set y-axis breaks to only show 0.0, 0.4, and 0.8\n  #scale_y_continuous(breaks = c(0.0, 0.4, 0.8)) +\n  scale_y_continuous(breaks = c(0.0, 0.5, 0.9, 1.0), limits = c(0, 1))+\n  \n  # Set x-axis breaks to include Blim and Btrigger explicitly\n  scale_x_continuous(breaks = c(0.0, 0.3, 0.5, 1.0)) +\n  \n  # Custom theme\n  #theme_minimal() +\n  \n  # Theme adjustments\n  theme(\n    # Titles and Axis Labels\n    plot.title = element_text(hjust = 0.5, face = \"bold\", size = 35, colour = Cyan),\n    axis.title = element_text(face = \"bold\", size = 35),\n    axis.text.x = element_text(size = 28, face = \"bold\"),\n    axis.text.y = element_text(size = 28, face = \"bold\"),\n    \n    # Legend Positioning and Style\n    legend.position = c(0.88, 0.8),  # Position legend inside the plot\n    legend.justification = c(1, 1),    \n    legend.title = element_blank(),    \n    legend.text = element_text(face = \"bold\", size = 8),  \n    legend.background = element_rect(fill = \"white\", colour = \"black\", linewidth = 0.5),\n    legend.key = element_rect(fill = \"white\"),\n    legend.key.height = unit(0.25, \"line\"),\n    legend.key.width = unit(0.8, \"line\"),\n    legend.key.size = grid::unit(0.2, \"lines\"),\n    legend.spacing.x = unit(0.05, \"cm\"),\n    legend.spacing.y = unit(0.05, \"cm\"),\n    \n    # Grid, Ticks, and Border\n    panel.grid.major = element_blank(),\n    panel.grid.minor = element_blank(),\n    panel.background = element_blank(),\n    panel.border = element_rect(fill = NA, colour = \"grey45\", linewidth = 4),\n    \n    # Bold Formatting for Various Text Elements\n    text = element_text(face = \"bold\"),\n    plot.subtitle = element_text(face = \"bold\"),\n    plot.caption = element_text(face = \"bold\")\n  ) +\n  theme(\n  axis.ticks = element_line(linewidth = 2, color = \"grey45\"),  # Makes axis ticks bolder\n  axis.ticks.length = grid::unit(20 / 2.2, \"pt\"),  # Increases tick length\n  \n  strip.background = element_rect(fill = \"grey45\", color = \"grey45\", linewidth = 2),  # Thicker strip lines\n  strip.text = element_text(face = \"bold\", size = rel(1.8))  # Increase strip text size and boldness\n)+\n  \n  # Combine linetype and color into a single legend frame\n  guides(\n    color = guide_legend(override.aes = list(linetype = 1)),\n    linetype = guide_legend(override.aes = list(color = Cyan))\n  ) +\n  \n  # Add mathematical formula annotation\n  annotation_custom(\n    grob = textGrob(\n      label = expression(F == 0.9 * F[MSY] * frac(B - B[lim], B[trigger] - B[lim])),\n      gp = gpar(fontsize = 25, fontface = \"italic\"),\n      hjust = 0, vjust = 1\n    ),\n    xmin = 0.5, xmax = 0.56, ymin = 0.3, ymax = 0.4  # Adjust the placement of the annotation\n  )\n```\n\n::: {.cell-output-display}\n![](MSE_files/figure-html/unnamed-chunk-9-1.png){width=100%}\n:::\n:::\n\n\n:::\n\n\n::: {.column width=\"32%\"}\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Load necessary libraries\nlibrary(ggplot2)\nlibrary(grid)\n\n# Define key biomass points\nBlim <- 0.3        # Limit biomass level (Blim)\nBtrigger <- 0.5    # Trigger biomass level (Btrigger)\nBmax <- 1.0        # Maximum biomass (100% of unfished biomass)\nFMSY <- 1.0        # Maximum sustainable fishing mortality\nFmin <- 0.5 * FMSY  # 50% of FMSY when below Blim\n\n# Create a sequence of biomass values\nB_values <- seq(0, Bmax, length.out = 100)\n\n# Compute fishing mortality based on the HCR E rule\nF_values <- ifelse(B_values < Blim, Fmin, \n                   ifelse(B_values <= Btrigger, \n                          Fmin + (FMSY - Fmin) * (B_values - Blim) / (Btrigger - Blim), \n                          FMSY))\n\n# Create a data frame\ndf <- data.frame(B_BMSY = B_values, F_FMSY = F_values / FMSY)\n\n# Generate the plot\nggplot(df, aes(x = B_BMSY, y = F_FMSY)) +\n  \n  # HCR E Curve\n  geom_line(color = teal, size = 5, aes(linetype = \"HCR Curve\")) +\n  \n  # Reference points as vertical dashed lines\n  geom_vline(aes(xintercept = Blim, color = \"Blim\"), linetype = \"dashed\", size = 2) +\n  geom_vline(aes(xintercept = Btrigger, color = \"Btrigger\"), linetype = \"dashed\", size = 2) +\n  \n  # Labels and legend\n  labs(\n    title = \"HCR E\", \n    x = expression(B  ~ bold(\"/\") ~  B[MSY]), \n    y = expression(F  ~ bold(\"/\") ~  F[MSY]),\n    linetype = \"HCR Components\", \n    color = NULL  # Remove color legend title\n  ) +\n  \n  # Scale colors for legend\n  scale_color_manual(values = c(\"Blim\" = \"red\", \n                                \"Btrigger\" = \"green\")) +\n  \n  # Set y-axis breaks to include 0.5 FMSY\n  scale_y_continuous(breaks = c(0.0, 0.5, 1.0), limits = c(0, 1))+\n  \n  # Set x-axis breaks to include Blim and Btrigger explicitly\n  scale_x_continuous(breaks = c(0.0, 0.3, 0.5, 1.0)) +\n  \n  # Custom theme\n  #theme_minimal() +\n  \n  # Theme adjustments\n  theme(\n    # Titles and Axis Labels\n    plot.title = element_text(hjust = 0.5, face = \"bold\", size = 35, colour = teal),\n    axis.title = element_text(face = \"bold\", size = 35),\n    axis.text.x = element_text(size = 28, face = \"bold\"),\n    axis.text.y = element_text(size = 28, face = \"bold\"),\n    \n    # Legend Positioning and Style\n    legend.position = c(0.88, 0.89),  # Position legend inside the plot\n    legend.justification = c(1, 1),    \n    legend.title = element_blank(),    \n    legend.text = element_text(face = \"bold\", size = 8),  \n    legend.background = element_rect(fill = \"white\", colour = \"black\", linewidth = 0.5),\n    legend.key = element_rect(fill = \"white\"),\n    legend.key.height = unit(0.25, \"line\"),\n    legend.key.width = unit(0.8, \"line\"),\n    legend.key.size = grid::unit(0.2, \"lines\"),\n    legend.spacing.x = unit(0.05, \"cm\"),\n    legend.spacing.y = unit(0.05, \"cm\"),\n    \n    # Grid, Ticks, and Border\n    panel.grid.major = element_blank(),\n    panel.grid.minor = element_blank(),\n    panel.background = element_blank(),\n    panel.border = element_rect(fill = NA, colour = \"grey45\", linewidth = 4),\n    \n    # Bold Formatting for Various Text Elements\n    text = element_text(face = \"bold\"),\n    plot.subtitle = element_text(face = \"bold\"),\n    plot.caption = element_text(face = \"bold\")\n  ) +\n  theme(\n  axis.ticks = element_line(linewidth = 2, color = \"grey45\"),  # Makes axis ticks bolder\n  axis.ticks.length = grid::unit(20 / 2.2, \"pt\"),  # Increases tick length\n  \n  strip.background = element_rect(fill = \"grey45\", color = \"grey45\", linewidth = 2),  # Thicker strip lines\n  strip.text = element_text(face = \"bold\", size = rel(1.8))  # Increase strip text size and boldness\n)+\n  \n  # Combine linetype and color into a single legend frame\n  guides(\n    color = guide_legend(override.aes = list(linetype = 1)),\n    linetype = guide_legend(override.aes = list(color = teal))\n  ) +\n  \n   # Add mathematical formula annotation\n  annotation_custom(\n    grob = textGrob(\n      label = expression(F == F[MSY] * (0.5 + 0.5 * frac(B - B[lim], B[trigger] - B[lim]))),\n      gp = gpar(fontsize = 25, fontface = \"italic\"),\n      hjust = 0, vjust = 1\n    ),\n    xmin = 0.35, xmax = 0.48, ymin = 0.3, ymax = 0.4  # Adjust the placement of the annotation\n  )\n```\n\n::: {.cell-output-display}\n![](MSE_files/figure-html/unnamed-chunk-10-1.png){width=100%}\n:::\n:::\n\n\n:::\n\n::::\n",
    "supporting": [
      "MSE_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {
      "include-in-header": [
        "<link href=\"site_libs/pagedtable-1.1/css/pagedtable.css\" rel=\"stylesheet\" />\n<script src=\"site_libs/pagedtable-1.1/js/pagedtable.js\"></script>\n"
      ]
    },
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}